# Copy to .env.local in this folder (Next loads it automatically):
#   cp env.local.template .env.local
#
# Dashboard proxies /api/v1/* to the Python API — must match where you run `agentiva serve`.
AGENTIVA_API_URL=http://127.0.0.1:8000
#
# Dev server: `npm run dev` uses --hostname 127.0.0.1 (avoids Next.js crashes on some VPN/Docker setups).
# Open http://127.0.0.1:3001 in the browser.
